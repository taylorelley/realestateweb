Based on the task description, we need to create a shell script that will build and run a Docker container. This script will use a Dockerfile that was already created. The Dockerfile is assumed to be in the same directory as the script.

Here are the main components we'll use:

1. Shell script: This will be used to execute the Docker commands for building the image and running the container.

Here is the content of the shell script:

build_and_run.sh
